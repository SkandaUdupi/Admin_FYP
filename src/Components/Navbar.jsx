import React from "react";
import { Box } from "@mui/material";

const Navbar = () => {
  return (
    <Box
      bgcolor="lightcoral"
      p={2}
      sx={{ display: { xs: "none", sm: "block" } }}
    >
      Navbar
    </Box>
  );
};

export default Navbar;
